{
  "name": "fetcher",
  "version": "1.0.0",
  "description": "A simple command-line tool built with TypeScript to fetch web pages and save them to disk for later retrieval and browsing. Additionally, it can extract and display metadata about the fetched pages",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.0",
    "commander": "^11.0.0",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.2",
    "@types/node": "^20.6.0",
    "typescript": "^5.2.2"
  }
}
